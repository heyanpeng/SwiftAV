/**
 * 视频缩放
 */

export class Resize {
  /**
   * 缩放视频帧
   */
  resize(frame: VideoFrame, width: number, height: number): VideoFrame {
    // TODO: 实现缩放逻辑
    return frame;
  }

  /**
   * 保持宽高比缩放
   */
  resizeWithAspectRatio(frame: VideoFrame, maxWidth: number, maxHeight: number): VideoFrame {
    // TODO: 实现保持宽高比的缩放逻辑
    return frame;
  }
}
