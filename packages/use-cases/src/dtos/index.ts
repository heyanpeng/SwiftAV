/**
 * DTOs（数据传输对象）
 * 
 * 用途：
 * 1. 作为 UseCase 的输入/输出，避免直接暴露领域实体
 * 2. 在不同层之间传输数据，适配不同的数据格式需求
 * 3. 序列化/反序列化，便于跨网络传输或持久化
 * 
 * 与实体的区别：
 * - Entity：包含业务逻辑，有方法，有业务规则
 * - DTO：纯数据，只有属性，用于传输
 */

export * from './TodoDTO';
